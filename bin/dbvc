#!/usr/bin/env php
<?php

use Jasny\DBVC\Console;

set_include_path(__DIR__ . '/src/' . PATH_SEPARATOR . get_include_path());

$loader = require_once dirname(__DIR__) .  DIRECTORY_SEPARATOR . 'vendor/autoload.php';
$loader->setUseIncludePath(true);

$console = new Console();
$console->run();
